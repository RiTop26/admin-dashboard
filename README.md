# admin-dashboard
In this project, I am going to build a full dashboard design using mostly CSS Grid for the layout.

As always, setting up a git repository for the project come first.

Then set up the HTML and CSS files with some dummy content.

Download all the necessary icons and images usable for the project

Build every section of the dashboard with the appropriate elements using skills from HTML and CSS